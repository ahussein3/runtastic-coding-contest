import {
  formatDistance
} from 'runtastic-coding-contest/helpers/format-distance';

module('FormatDistanceHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDistance(42);
  ok(result);
});
