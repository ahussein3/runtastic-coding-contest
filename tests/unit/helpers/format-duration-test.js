import {
  formatDuration
} from 'runtastic-coding-contest/helpers/format-duration';

module('FormatDurationHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDuration(42);
  ok(result);
});
